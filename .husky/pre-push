#!/bin/sh
# Quick sanity check before push: typecheck + frontend tests (no external deps needed).
# For the full integration suite (requires Redis, PostgreSQL, Portainer), run manually:
#   npm run test:integration
npm run typecheck && npm run test -w frontend
