// @dashboard/foundation — foundational, cross-domain routes that are tightly
// coupled to the Portainer API and core services (the "glue" layer between the
// Portainer API, core services, and the frontend).
//
// Routes are NOT re-exported from this package barrel (core/src/CLAUDE.md rule).
// Register them via `@dashboard/foundation/routes/index.js` in the composition
// root, consistent with every other package (#1533).
export {};
